<%= form_for(flashvendor) do |f| %>
  <% if flashvendor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flashvendor.errors.count, "error") %> prohibited this flashvendor from being saved:</h2>

      <ul>
      <% flashvendor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :vendor_name %>
    <%= f.text_field :vendor_name %>
  </div>

  <div class="field">
    <%= f.label :vendor_description %>
    <%= f.text_area :vendor_description %>
  </div>

  <div class="field">
    <%= f.label :vendor_url %>
    <%= f.text_field :vendor_url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
